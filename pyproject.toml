[project]
name = "openenv-email-triage"
version = "0.1.0"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openai",
    "openenv>=0.2.0"
]

[project.scripts]
server = "app:app"

[tool.openenv]
entrypoint = "app.py"
