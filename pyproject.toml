[project]
name = "openenv-email-triage"
version = "0.1.0"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openai",
    "openenv==0.1.13"
]

[project.scripts]
server = "server.app:main"

[tool.openenv]
entrypoint = "app.py"
