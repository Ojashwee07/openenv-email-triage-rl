name: email_triage_env
version: 1.0

entrypoint: app.py

observation:
  email_text: string
  sender: string
  urgency: int
  step_count: int

action:
  action_type: string

reward:
  value: float

tasks:
  - id: easy
    grader: graders.grade_easy
  - id: medium
    grader: graders.grade_medium
  - id: hard
    grader: graders.grade_hard
